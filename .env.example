# WhaleTracker AI - Environment Variables
# Copy this file to .env and fill in your credentials.
# NEVER commit .env to version control.

# --- Financial Data API ---
# Option A: Financial Modeling Prep (https://financialmodelingprep.com/developer/docs)
FMP_API_KEY=your_fmp_api_key_here

# Option B: Fintel API (https://fintel.io/api)
FINTEL_API_KEY=your_fintel_api_key_here

# --- Slack Bot ---
# Create app at https://api.slack.com/apps → Bot Token Scopes: chat:write
SLACK_BOT_TOKEN=xoxb-your-bot-token-here
SLACK_ALERT_CHANNEL=#whale_tracker_alerts

# --- Alert Scheduler ---
# How often to re-fetch SEC data and check for new signals (default: 4 hours)
REFRESH_INTERVAL_HOURS=4
# UTC hour to send the morning digest (default: 8 = 08:00 UTC)
DAILY_DIGEST_HOUR=8
# Comma-separated tickers to watch for conviction score changes
ALERT_WATCHLIST=AAPL,NVDA,GOOGL
# Minimum conviction score delta to trigger a watchlist alert (default: 2)
ALERT_MIN_SCORE=2

# --- Form 4 Real-Time Alerts (Phase 3) ---
# Tickers to monitor for insider trades (near real-time, every 30 min)
# Falls back to ALERT_WATCHLIST if not set
FORM4_WATCH_TICKERS=AAPL,MSFT,NVDA,META,GOOGL
# How often to poll EDGAR for new Form 4 filings (minutes, default: 30)
FORM4_REFRESH_MINUTES=30

# --- Macro Dashboard (Phase 4) ---
# FRED API key — free registration at https://fred.stlouisfed.org/docs/api/api_key.html
# If not set, the dashboard shows realistic mock data
FRED_API_KEY=your_fred_api_key_here

# --- News Digest (Phase 5) ---
# NewsAPI.org key — free tier at https://newsapi.org (100 req/day)
# If not set, RSS fallback (MarketWatch / Yahoo Finance) is used automatically
NEWS_API_KEY=your_newsapi_key_here

# --- Standalone Alert Daemon ---
# Run independently of the web app: python alert_daemon.py
# Requires SLACK_BOT_TOKEN + FORM4_WATCH_TICKERS (or ALERT_WATCHLIST)

# --- Firebase (User Auth + Cloud Portfolio Sync) ---
# Free tier is sufficient. Create project at https://console.firebase.google.com
# Enable: Authentication → Email/Password sign-in
#         Firestore Database (start in production mode, add rules below)
#
# Firestore security rules (paste in Firebase Console → Firestore → Rules):
#   match /portfolios/{userId} {
#     allow read, write: if request.auth != null && request.auth.uid == userId;
#   }
#
# Web API Key: Firebase Console → Project Settings → General → Web API Key
FIREBASE_API_KEY=your_firebase_web_api_key_here
# Project ID: Firebase Console → Project Settings → General → Project ID
FIREBASE_PROJECT_ID=your_firebase_project_id_here

# --- App Config ---
# Set to "mock" to use sample data without real API calls
DATA_MODE=mock   # options: mock | live

# Cache TTL in seconds (default: 6 hours)
CACHE_TTL_SECONDS=21600
